//using loops
int binarySearch(int* arr,int size,int key){
    int left = 0 , right = size - 1,mid;

    while(left < right){
        mid = (left + right)/2;

        if(key == arr[mid]){
            return mid;
        }
        else if(key < arr[mid]){
            //left array
            right 
        }else{
            //right array

        }
    }
}